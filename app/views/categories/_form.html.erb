<%= form_for(@category) do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
  <div id ="login">
  	<table>
    	<tr>
  	  	<td><%= f.text_field :name %></td>
  		</tr>
  	</table>
  </div>
  <div class="actions">
		<%= f.submit 'speichern' %>
	</div>
<% end %>