<%= form_for(@subcategory) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% @subcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
		<%= f.label :category_id, "Kategorie" %>
	    	<%= f.select :category_id,
				Category.find(:all).collect{|d| [d.name, d.id]},
			 	:prompt => "Bitte waehlen" %>
	</div>
  <div>
	  <%= f.label :name %>
  	<%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "speichern" %>
  </div>
<% end %>